#include <stdio.h>
/**
 * main - Entry point of the program.
 * This function outputs a message followed by a new line using the printf.
 * Return: 0 (success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
